import { colors } from '../theme/theme';

export const paperInputProps = {
  mode: 'outlined',
  activeOutlineColor: '#7dd3fc',
  outlineColor: colors.outline,
  theme: { roundness: 14, colors: { background: 'rgba(255,255,255,0.04)' } },
};

